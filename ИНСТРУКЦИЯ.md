# 🚗 Инструкция по запуску сайта "Автосалон"

## 📋 Содержание
1. [Требования к системе](#требования-к-системе)
2. [Перенос проекта на другое устройство](#перенос-проекта-на-другое-устройство)
3. [Установка и настройка](#установка-и-настройка)
4. [Запуск сайта](#запуск-сайта)
5. [Доступ к сайту](#доступ-к-сайту)
6. [Структура проекта](#структура-проекта)
7. [Возможные проблемы](#возможные-проблемы)

---

## 📦 Требования к системе

### Минимальные требования:
- **ОС**: Windows 10/11, Linux, macOS
- **Python**: версия 3.8 или выше
- **Оперативная память**: минимум 2 ГБ
- **Свободное место**: минимум 500 МБ

### Необходимое ПО:
- Python 3.8+ ([скачать](https://www.python.org/downloads/))
- pip (устанавливается вместе с Python)
- Браузер (Chrome, Firefox, Edge и т.д.)

---

## 📂 Перенос проекта на другое устройство

### Вариант 1: Копирование всей папки
1. Скопируйте всю папку `test` на новое устройство
2. Убедитесь, что скопированы все файлы и папки:
   - `app.py`
   - `config.py`
   - `db_utils.py`
   - `data.db`
   - `requirements.txt`
   - `run.bat`
   - `README.md`
   - папка `templates/`
   - папка `static/`
   - папка `.venv/` (опционально, можно создать заново)

### Вариант 2: Через Git (рекомендуется)
```bash
# На новом устройстве (клонировать проект)
git clone https://github.com/DmitriyPrivalov/autosalon-flask.git
cd autosalon-flask

# Создать виртуальное окружение
python -m venv .venv

# Активировать окружение
.venv\Scripts\Activate.ps1

# Установить зависимости
pip install -r requirements.txt

# Создать базу данных с тестовыми данными
python init_db.py

# Запустить сайт
python app.py
```

### Вариант 3: Через архив
1. Упакуйте папку проекта в ZIP/RAR архив
2. Перенесите архив на новое устройство
3. Распакуйте в удобное место

⚠️ **Важно**: Папку `.venv/` можно не переносить - она будет создана автоматически при установке

---

## 🔧 Установка и настройка

### Шаг 1: Проверка установки Python

Откройте командную строку (PowerShell на Windows) и выполните:

```powershell
python --version
```

Должна отобразиться версия Python 3.8 или выше. Если нет - установите Python с [официального сайта](https://www.python.org/downloads/).

### Шаг 2: Переход в папку проекта

```powershell
cd путь\к\папке\test
```

Пример:
```powershell
cd C:\Users\YourName\Desktop\test
```

### Шаг 3: Создание виртуального окружения

```powershell
python -m venv .venv
```

Эта команда создаст папку `.venv` с изолированным окружением Python.

### Шаг 4: Активация виртуального окружения

**На Windows (PowerShell):**
```powershell
.venv\Scripts\Activate.ps1
```

**На Windows (CMD):**
```cmd
.venv\Scripts\activate.bat
```

**На Linux/macOS:**
```bash
source .venv/bin/activate
```

После активации в начале строки появится `(.venv)`.

### Шаг 5: Установка зависимостей

```powershell
pip install -r requirements.txt
```

Эта команда установит все необходимые библиотеки:
- Flask 2.3.3
- Werkzeug 2.3.7
- flasgger (для Swagger документации)

### Шаг 6: Инициализация базы данных

```powershell
python init_db.py
```

Этот скрипт автоматически:
- ✅ Создаст базу данных `data.db`
- ✅ Создаст все необходимые таблицы
- ✅ Добавит тестовые автозапчасти (12 товаров)
- ✅ Создаст администратора (admin/123)
- ✅ Создаст тестового пользователя (TestUser123/testpass123)
- ✅ Добавит тестовые салоны и заказы

⚠️ **Важно**: Если база уже существует, скрипт предложит удалить её и создать новую.

---

## 🚀 Запуск сайта

### Способ 1: Через готовый скрипт (Windows)

Просто дважды кликните на файл `run.bat` в папке проекта.

### Способ 2: Через командную строку

1. Откройте PowerShell/CMD
2. Перейдите в папку проекта
3. Активируйте виртуальное окружение (если еще не активировано)
4. Запустите приложение:

```powershell
python app.py
```

### Ожидаемый вывод при успешном запуске:

```
==================================================
Автосалон на Python Flask
==================================================
🚀 Приложение запускается на http://localhost:5000
💾 База данных: data.db
📁 Статические файлы: static/
==================================================
 * Serving Flask app 'app'
 * Debug mode: on
 * Running on http://localhost:5000
 * Debugger is active!
```

✅ Если видите такой вывод - сайт работает!

---

## 🌐 Доступ к сайту

После запуска откройте браузер и перейдите по следующим адресам:

### Основные страницы:

| Страница | URL | Описание |
|----------|-----|----------|
| **Главная** | http://localhost:5000 | Домашняя страница сайта |
| **Каталог товаров** | http://localhost:5000/catalog | Список всех автозапчастей |
| **Салоны** | http://localhost:5000/showrooms | Информация о салонах |
| **Личный кабинет** | http://localhost:5000/cabinet | Личный кабинет пользователя |
| **Вход** | http://localhost:5000/login | Страница входа |
| **Регистрация** | http://localhost:5000/register | Регистрация нового пользователя |

### Административные страницы:

| Страница | URL | Требуется авторизация |
|----------|-----|----------------------|
| **Управление товарами** | http://localhost:5000/admin/products | ✅ Админ |
| **Управление услугами** | http://localhost:5000/admin/services | ✅ Админ |
| **Управление заказами** | http://localhost:5000/admin/orders | ✅ Админ |
| **Управление пользователями** | http://localhost:5000/admin/users | ✅ Админ |

### 📚 API Swagger документация:

| Ресурс | URL | Описание |
|--------|-----|----------|
| **Swagger UI** | http://localhost:5000/api/docs/ | Интерактивная документация API |
| **JSON спецификация** | http://localhost:5000/apispec.json | OpenAPI спецификация в JSON |

#### Что такое Swagger?
Swagger - это интерфейс для тестирования API. Здесь вы можете:
- Посмотреть все доступные API endpoints
- Протестировать запросы прямо в браузере
- Увидеть структуру запросов и ответов
- Проверить параметры и коды ответов

#### Как использовать Swagger:
1. Откройте http://localhost:5000/api/docs/
2. Выберите интересующий endpoint (например, `/api/products`)
3. Нажмите "Try it out"
4. Заполните параметры (если требуется)
5. Нажмите "Execute"
6. Просмотрите результат

---

## 📁 Структура проекта

```
test/
│
├── 📄 app.py                    # Главный файл приложения Flask
├── 📄 config.py                 # Файл конфигурации
├── 📄 db_utils.py               # Утилиты для работы с базой данных
├── 📄 init_db.py                # Скрипт инициализации БД
├── 📄 data.db                   # База данных SQLite (создается автоматически)
├── 📄 requirements.txt          # Список зависимостей Python
├── 📄 run.bat                   # Скрипт запуска (Windows)
├── 📄 .gitignore                # Исключения для Git
├── 📄 README.md                 # Основная документация
├── 📄 ИНСТРУКЦИЯ.md             # Этот файл
│
├── 📁 templates/                # HTML шаблоны
│   ├── index.html              # Главная страница
│   ├── catalog.html            # Каталог товаров
│   ├── showrooms.html          # Салоны
│   ├── cabinet.html            # Личный кабинет
│   ├── login.html              # Страница входа
│   ├── register.html           # Регистрация
│   ├── admin_products.html     # Админ: товары
│   ├── admin_services.html     # Админ: услуги
│   ├── admin_orders.html       # Админ: заказы
│   └── admin_users.html        # Админ: пользователи
│
├── 📁 static/                   # Статические файлы
│   ├── css/                    # Стили
│   │   ├── style.css
│   │   ├── media.css
│   │   ├── avtor.css
│   │   └── registr.css
│   ├── img/                    # Изображения
│   └── ...                     # Другие статические файлы
│
└── 📁 .venv/                    # Виртуальное окружение Python
    └── ...
```

---

## 🔑 Учетные данные по умолчанию

### Администратор:
- **Логин**: `admin`
- **Пароль**: `admin123`

### Тестовый пользователь:
- **Логин**: `user`
- **Пароль**: `user123`

⚠️ **Важно**: Смените пароли после первого входа!

---

## ❌ Остановка сервера

Чтобы остановить работающий сервер:

1. Перейдите в окно терминала/PowerShell с запущенным приложением
2. Нажмите **Ctrl + C**
3. Подтвердите остановку (если требуется)

---

## ⚠️ Возможные проблемы и решения

### Проблема 1: "Python не найден" или "Python is not recognized"

**Решение:**
1. Убедитесь, что Python установлен: скачайте с [python.org](https://www.python.org/downloads/)
2. При установке отметьте "Add Python to PATH"
3. Перезапустите PowerShell/CMD после установки

### Проблема 2: Ошибка активации виртуального окружения на Windows

**Ошибка:**
```
.venv\Scripts\Activate.ps1 : Невозможно загрузить файл... выполнение сценариев отключено в этой системе
```

**Решение:**
Запустите PowerShell от имени администратора и выполните:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

Затем попробуйте активировать окружение снова.

### Проблема 3: Порт 5000 уже занят

**Ошибка:**
```
Address already in use
```

**Решение 1** - Остановить процесс на порту 5000:
```powershell
# Найти процесс
netstat -ano | findstr :5000

# Остановить процесс (замените PID на номер из предыдущей команды)
taskkill /PID <PID> /F
```

**Решение 2** - Изменить порт в файле `app.py`:
Найдите в конце файла:
```python
app.run(debug=True, host='0.0.0.0', port=5000)
```
Измените на:
```python
app.run(debug=True, host='0.0.0.0', port=5001)
```

### Проблема 4: Ошибка "No module named 'flask'"

**Решение:**
```powershell
# Убедитесь, что виртуальное окружение активировано
.venv\Scripts\Activate.ps1

# Установите зависимости
pip install -r requirements.txt
```

### Проблема 5: База данных не найдена

**Решение:**
База данных `data.db` должна находиться в корне проекта. Если её нет, она будет создана автоматически при первом запуске приложения.

### Проблема 6: Статические файлы не загружаются (404 ошибка)

**Решение:**
Убедитесь, что папка `static/` и все её содержимое находятся в корне проекта.

### Проблема 7: Не могу войти в админ-панель

**Решение:**
1. Убедитесь, что вы используете правильные учетные данные (admin/admin123)
2. Проверьте, что в базе данных есть пользователь с правами администратора
3. Попробуйте очистить cookies браузера

---

## 🔄 Обновление проекта

### При переносе обновленной версии:

1. Скопируйте новые файлы в папку проекта
2. Обновите зависимости:
```powershell
pip install -r requirements.txt --upgrade
```
3. Перезапустите сервер

---

## 💡 Полезные команды

### Просмотр установленных пакетов:
```powershell
pip list
```

### Обновление pip:
```powershell
python -m pip install --upgrade pip
```

### Создание нового файла requirements.txt:
```powershell
pip freeze > requirements.txt
```

### Проверка структуры базы данных:
```powershell
python -c "import sqlite3; conn = sqlite3.connect('data.db'); print(conn.execute('SELECT name FROM sqlite_master WHERE type=\"table\"').fetchall())"
```

---

## 📞 Контакты и поддержка

Если возникли проблемы:
1. Проверьте раздел "Возможные проблемы"
2. Убедитесь, что все зависимости установлены
3. Проверьте логи в терминале
4. Попробуйте перезапустить приложение

---

## 📝 Дополнительная информация

### Режим разработки (Debug Mode)
По умолчанию приложение запускается в режиме отладки. В этом режиме:
- ✅ Автоматическая перезагрузка при изменении файлов
- ✅ Подробные сообщения об ошибках
- ⚠️ **НЕ используйте в продакшене!**

### Режим продакшена
Для запуска в продакшене используйте WSGI сервер (например, Gunicorn или Waitress).

---

## ✅ Чек-лист перед переносом

- [ ] Python 3.8+ установлен на новом устройстве
- [ ] Скопированы все файлы проекта
- [ ] Создано виртуальное окружение
- [ ] Установлены зависимости из requirements.txt
- [ ] База данных data.db присутствует
- [ ] Папки templates/ и static/ на месте
- [ ] Порт 5000 свободен
- [ ] Браузер готов для тестирования

---

**Удачи в работе с проектом! 🚀**

*Последнее обновление: 23 декабря 2025 г.*
